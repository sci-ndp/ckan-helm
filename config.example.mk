# Sample overrides for the CKAN Makefile. Copy this file to `config.mk`
# and adjust the values to match your deployment environment. All fields
# are optional; command-line overrides still take precedence.

KUBE_CONTEXT = 
HELM_RELEASE = ckan
NAMESPACE = ckan
